# 现场任务单（宣布任务时投屏这个文件）

日志样本：`access.log`（当前 35 行，现场大文件版可用 `gen-big-log.sh` 生成）

---

## 任务 1：Top 5 IP

统计出现次数最多的前 5 个 IP，输出两列：`次数 IP`

## 任务 2：故障提取

只提取 **status=500** 的行里的「时间戳 + 接口名」，制表符分隔：

```
2026-09-12 14:23:53	POST /api/orders
```

## 任务 3：配置替换

把 `config.properties` 里所有 `env=test` 替换为 `env=prod`：
- 必须先自动备份原文件
- 替换后展示 diff

---

### 计时规则

- 传统组：只许用 man 和搜索引擎，不许用 AI
- AI 组：不许用搜索引擎，只许对 AI 描述需求

> 预期：传统组 30 分钟起步；AI 组 1 分钟内交卷。
